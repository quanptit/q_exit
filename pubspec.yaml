name: q_exit
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  q_common_utils:
    path: ../q_common_utils
  q_theme: 
    path: ../q_theme
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  plugin:
    platforms:
      android:
        package: com.app.exitads
        pluginClass: QExitLibPlugin